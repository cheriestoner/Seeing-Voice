[project]
name = "seeing-sound"
version = "0.1.0"
description = "Seeing Sound - A high-performance, real-time spectrogram visualization web application"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["flask>=3.0.0"]

[project.optional-dependencies]
dev = ["pytest"]
